<p id="notice"><%= notice %></p>

<p>
  <%= @check_list_definition.name %>
  <br/>
  <%= @check_list_definition.description %>
</p>

<p>
  <strong>Check List:</strong>
  <ul class="list-unstyled">
    <% @check_list_definition.task_definitions.each do |task_definition| %>
      <li><%= task_definition.text %></li>
    <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_check_list_definition_path(@check_list_definition) %> |
<%= link_to 'Back', check_list_definitions_path %>
