<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>User</th>
      <th>Current Step</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @check_lists.each do |check_list| %>
      <tr>
        <td><%= check_list.name %></td>
        <td><%= check_list.user %></td>
        <td>TO BE IMPLEMENTED</td>
        <td><%= link_to 'Resume', edit_check_list_path(check_list), class: "btn btn-primary" %></td>
        <td><%= link_to 'Cancel', check_list, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-default" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
